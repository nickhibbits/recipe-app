import React from "react";

function RestaurantProfile() {
  return (
    <div>
      RestaurantProfile - page containing restaurant details and reviews -- make
      area to write review and an area to see all reviews
    </div>
  );
}

export default RestaurantProfile;
