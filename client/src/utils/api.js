async function getRecipes() {
  // get recipes from express server, which calls Spoonacular API
  // how best to organize recipes?
  // // different fetch call for each style of food, vs one function to get all recipes?
  // // OR get all recipes at once and sort them on the client?
}

async function getUsers() {
  // get all users
  // create login flow with password authentication that checks against existing users
  // make mock data to start, migrate to mongo db for practice with real database
}
